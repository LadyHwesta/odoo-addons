<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_hestiacp_account_form" model="ir.ui.view">
        <field name="name">hestiacp.account.form</field>
        <field name="model">hestiacp.account</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_provision" type="object" string="Provision"
                            class="btn-primary" invisible="state != 'draft'" />
                    <button name="action_suspend" type="object" string="Suspend"
                            invisible="state != 'active'" />
                    <button name="action_unsuspend" type="object" string="Unsuspend"
                            invisible="state != 'suspended'" />
                    <button name="action_terminate" type="object" string="Terminate"
                            confirm="This deletes the account on HestiaCP. Continue?"
                            invisible="state not in ('active', 'suspended')" />
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,active,suspended,terminated" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="username" placeholder="Generated on provisioning" /></h1>
                    </div>
                    <group>
                        <group>
                            <field name="partner_id" />
                            <field name="product_id" />
                            <field name="server_id" />
                        </group>
                        <group>
                            <field name="sale_order_id" />
                            <field name="contract_id" />
                            <field name="control_panel_url" widget="url" />
                            <field name="payment_token_id" />
                        </group>
                    </group>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_hestiacp_account_list" model="ir.ui.view">
        <field name="name">hestiacp.account.list</field>
        <field name="model">hestiacp.account</field>
        <field name="arch" type="xml">
            <list>
                <field name="username" />
                <field name="partner_id" />
                <field name="product_id" />
                <field name="server_id" />
                <field name="state" widget="badge"
                       decoration-success="state == 'active'"
                       decoration-warning="state == 'suspended'"
                       decoration-muted="state in ('draft', 'terminated')" />
            </list>
        </field>
    </record>

    <record id="action_hestiacp_account" model="ir.actions.act_window">
        <field name="name">Hosting Accounts</field>
        <field name="res_model">hestiacp.account</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
