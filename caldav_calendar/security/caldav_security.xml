<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="caldav_account_rule_own" model="ir.rule">
        <field name="name">CalDAV Account: own records only</field>
        <field name="model_id" ref="model_caldav_account"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
    <record id="caldav_pending_delete_rule_own" model="ir.rule">
        <field name="name">CalDAV Pending Delete: own records only</field>
        <field name="model_id" ref="model_caldav_pending_delete"/>
        <field name="domain_force">[('account_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
